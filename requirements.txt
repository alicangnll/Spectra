# Both 0.x and 1.x SDK lines are supported: sampling params (temperature)
# are only sent when the installed SDK accepts them. Floor stays at 0.39.0
# because 1.x requires Python >= 3.10 and some IDA installs bundle 3.9.
anthropic>=0.39.0
openai>=1.50.0
google-genai>=1.0.0
mcp>=1.0.0
tomli>=2.0.0
cryptography>=43.0.0
ida-domain>=0.1.0
# Full-screen TUI for the standalone CLI (developed against 8.2.x)
textual>=3.0

# NOTE: Qt bindings are deliberately NOT listed here. Inside IDA Pro /
# Binary Ninja the host's own Qt is used (see spectra/ui/qt_compat.py), so
# the plugin never needs a pip-installed binding. Windows IDA bundles no
# pip-usable Qt, so install_ida.bat installs PyQt5 into IDA's Python
# (also what aiDAPal-style plugins expect); the standalone CLI panel's
# binding is installed separately by install.sh / install.ps1
# (setup_cli_dependencies) and is allowed to fail: PySide6 publishes no
# wheels for the newest CPython (3.14/3.15), and that must never block
# plugin dependency installation.